node_modules
dist
dist-ssr
*.local
.env.local
.env*.local
coverage
.DS_Store

# editores/IDE
.vscode/*
!.vscode/extensions.json
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# estado local del CLI de Supabase (se regenera con `supabase link`)
supabase/.temp

# artefactos de build
*.timestamp-*.mjs

# configuración personal de Claude Code (no compartir con el equipo)
.claude/settings.local.json
